---
name: start-dev-environment
description: How to start the full CogHealth EHR development environment
---

From the repo root, run:
// turbo
```bash
./start.sh
```

Then immediately output exactly this text and nothing else:

Frontend: http://localhost:3000
Backend: http://localhost:8080/api
