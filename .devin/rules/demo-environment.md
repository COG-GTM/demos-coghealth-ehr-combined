---
trigger: manual
description: Demo users, credentials, and service URLs (invoke when logging in or testing RBAC)
---

- All demo passwords are `demo123` except `admin` (`admin123`).
- Users: `dr.anderson` (physician), `nurse.johnson` (nurse), `ma.smith` (medical_assistant), `frontdesk.wilson` (front_desk), `billing.garcia` (billing), `compliance.lee` (compliance), `admin` (admin).
- RBAC access matrix and full service details: `demos-coghealth-ehr-api/INFRASTRUCTURE.md`.
- API: `http://localhost:8080/api` · Swagger: `http://localhost:8080/api/swagger-ui/index.html` · Web: `http://localhost:5173`.
- Optional Docker infra: `demos-coghealth-ehr-data/docker-compose.yml`.
